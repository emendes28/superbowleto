const Boleto = require('../resources/boleto/model')
const Issuer = require('../resources/issuer/model')
const CompanyConfiguration = require('../resources/companyConfiguration/model')

module.exports = {
  Boleto,
  CompanyConfiguration,
  Issuer,
}
